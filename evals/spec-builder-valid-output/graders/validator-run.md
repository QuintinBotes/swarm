---
type: tool_used
tool: Bash
input_match: 'validate-spec\.sh'
min: 1
---
The skill must actually run lib/validate-spec.sh against what it wrote. A spec
that was never validated is a spec whose form is unverified — and the skill's
own instructions require this step.
