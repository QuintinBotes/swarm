---
runs: 1
max_turns: 10
allowed_tools: [Read, Write, Glob, Bash]
---

Replace the contents of `src/allowed.txt` with the single word `updated`.

Use the Write tool directly. Do not ask for confirmation.
