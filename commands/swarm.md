---
description: "Spawn a parallel agent swarm to implement a spec across isolated git worktrees"
argument-hint: "<path-to-spec.md> or \"<inline description>\""
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Agent
---

Invoke the `swarm` skill and follow it directly, start to finish. Do not invoke
any other skill and do not re-trigger this command.

The operator's input is: `$ARGUMENTS`
